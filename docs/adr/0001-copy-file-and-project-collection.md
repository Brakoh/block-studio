# Copy YAML vs Project collection

Site Copy lives in one YAML file so the designer can change labels, paragraphs, and inscriptions in a single place. Selected Work is a growing list of Projects, so each Project is its own Markdown file in an Astro collection instead of rows inside that YAML. Mixing the two would either freeze the work list or turn the Copy file into a second database.
